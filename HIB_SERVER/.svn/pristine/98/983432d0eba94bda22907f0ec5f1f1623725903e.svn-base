#define TEST_SWITCH_OPEN 

#include <windows.h>
#include "../net/socket/HardSocket.h"

int main(int argc, char *args[]) 
{
    
	try 
	{
		HardSocket socket;
		//socket.init();
	}
	catch (SocketException e) 
	{
	    printf("socket init error: %d, messsage: %s\n", e.getCode(), e.getMessage());
	}
	return 0;
}