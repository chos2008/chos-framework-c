/*
 *
 *
 * @author ada
 * @version 1.0
 * @since 1.0
 */
#if ! defined RUNNABLE
#define RUNNABLE

/**
 * This class is the base interface to implement a thread. Someone to 
 * create a thread through it must implement this interface, and 
 * implement {@link #run()} method.
 *
 * Someone can also create thread through class {@link Thread}, and 
 * overlands method {@link #run()}. Pls see {@link Thread} for more details.
 *
 * @see {@link Thread} 
 */
class /*TK_API*/ Runnable {

protected:


public:

	virtual void run() = 0;
};

#endif