/*
 * DOCUMENT!
 *
 * @author a.da
 * @since 1.0
 */

/**
 * random select a number from n numbers, the random 
 * selected number is between zero and n - 1. 
 *
 * @param n  
 * @param seed  seed
 * @see random_select(int n)
 * @see random_select_t(int n)
 */
int random_select_with_seed(int n, int seed);

/**
 * random select a number from n numbers, the random 
 * selected number is between zero and n - 1. 
 *
 * @see random_select_t(int n)
 * @see random_select_with_seed(int n, int seed)
 */
int random_select(int n);

/**
 * random select a number from n numbers, the random 
 * selected number is between zero and n - 1. 
 *
 * @see random_select(int n)
 * @see random_select_with_seed(int n, int seed)
 */
int random_select_t(int n);
